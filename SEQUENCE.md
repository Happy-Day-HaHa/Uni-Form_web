# UNI-FORM 동작 흐름

## 사용자 흐름

1. 사용자는 일반 이메일과 비밀번호로 로그인하거나 회원가입합니다.
2. 회원가입에서는 이름, 직업, 나이, 생년월일, 성별과 기타 기본정보를 입력할 수 있습니다.
3. 로그인 후 메인 설문 목록에서 제목, 설명, 예상 소요시간, 응답 현황과 참여 가능 여부를 확인합니다.
4. 다른 사용자의 설문은 응답 화면으로, 내가 만든 설문은 응답 현황 또는 결과 화면으로 연결됩니다.
5. `SurveyCreate.jsx`는 왼쪽 직접 편집 영역과 오른쪽 `Uni-Chat` 제안 영역으로 구성됩니다.
6. 사용 목적을 입력하면 질문, 보기, 순서와 표현 초안을 만들고 설문 편집 영역에 반영합니다.
7. 사용자는 설문을 미리 본 뒤 등록하며, 등록된 설문은 설문 목록에 바로 노출됩니다.
8. 응답자는 설문을 제출하고 응답 데이터는 설문별로 저장됩니다.
9. 설문 제작자는 응답이 한 건 이상 들어온 자신의 설문 결과만 확인할 수 있습니다.
10. 결과 화면은 전체 응답 수, 문항별 분포, 주관식 응답과 핵심 요약을 제공합니다.

## 폴더 역할

- `pages/`: URL별 화면과 사용자 상호작용
- `components/`: 여러 화면에서 재사용하는 UI와 설문 캐러셀
- `services/`: 인증, 설문, 응답과 프로필 데이터 호출
- `hooks/`: 로그인 세션 상태
- `utils/`: 조건 매칭과 입력 검증
- `routes/`: 공개/보호 라우트
- `database/`: 사용자, 설문과 응답 데이터 구조

## 데이터 관계

```text
auth.users 1 ── 1 public.users
public.users 1 ── N surveys
public.users 1 ── N responses
surveys     1 ── N responses
```

회원 기본정보는 설문 추천과 응답자 특성별 결과 분석에 사용합니다. 응답 결과는 설문 제작자에게만 제공하며, 응답이 없는 설문은 결과 분석 화면을 열지 않습니다.
