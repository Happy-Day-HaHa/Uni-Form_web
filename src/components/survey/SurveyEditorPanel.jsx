export default function SurveyEditorPanel({ form, onChange, onQuestionChange, onAddQuestion, onDeleteQuestion, aiApplied = false }) {
  return <div className="create-saas__editor">
    <section className="create-editor-card"><header><div><span>▤</span><h2>설문 기본 정보</h2></div>{aiApplied && <b>● AI 반영 완료</b>}</header><div className="create-editor-fields"><label>설문 제목 <em>*</em><input value={form.title} onChange={(event) => onChange({ title: event.target.value })} placeholder="설문 제목을 입력하세요." /></label><label>설문 설명<textarea rows="3" value={form.description} onChange={(event) => onChange({ description: event.target.value })} placeholder="응답자가 설문 목적을 이해할 수 있도록 적어주세요." /></label><div className="create-editor-grid"><label>예상 소요 시간<select value={form.estimatedMinutes} onChange={(event) => onChange({ estimatedMinutes: event.target.value })}>{[3, 5, 7, 10].map((item) => <option value={item} key={item}>약 {item}분</option>)}</select></label><label>모집 인원<input type="number" min="1" value={form.targetCount} onChange={(event) => onChange({ targetCount: event.target.value })} /></label><label>설문 대상<select value={form.ageGroup} onChange={(event) => onChange({ ageGroup: event.target.value })}>{['전체', '10대', '20대', '30대', '40대', '50대 이상'].map((item) => <option key={item}>{item}</option>)}</select></label><label>공개 범위<select value={form.visibility} onChange={(event) => onChange({ visibility: event.target.value })}><option>전체 공개</option><option>조건별 공개</option><option>비공개</option></select></label></div></div></section>
    <section className="create-editor-card create-editor-card--questions"><header><div><span>▦</span><h2>문항 구성</h2></div><b>총 {form.questions.length}개의 문항</b></header><div className="create-question-list">{form.questions.map((question, index) => <article className="create-question" key={question.id}><span>Q{index + 1}</span><div><input value={question.title} onChange={(event) => onQuestionChange(question.id, { title: event.target.value })} placeholder="질문을 입력하세요." /><div><select value={question.type} onChange={(event) => onQuestionChange(question.id, { type: event.target.value })}><option value="text">주관식</option><option value="single">객관식</option><option value="scale">척도형</option></select>{question.type === 'single' && <input value={question.options.join(', ')} onChange={(event) => onQuestionChange(question.id, { options: event.target.value.split(',').map((item) => item.trim()).filter(Boolean) })} placeholder="선택지를 쉼표로 구분" />}{question.type === 'scale' && <small>1 매우 낮음 — 5 매우 높음</small>}</div></div>{form.questions.length > 1 && <button type="button" aria-label="질문 삭제" onClick={() => onDeleteQuestion(question.id)}>×</button>}</article>)}</div><button className="create-add-question" type="button" onClick={onAddQuestion}>＋ 문항 추가하기</button></section>
  </div>
}
